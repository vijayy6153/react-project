# React Admin Demo

## Quick start

1. Install dependencies:
   ```bash
   npm install
   ```
2. Run dev server:
   ```bash
   npm run dev
   ```
3. Open the URL shown by Vite (usually http://localhost:5173)

This demo includes a simple front layout and an admin layout with routing.
Admin pages are accessible at `/admin`.
